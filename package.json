{
  "name": "immersive-bilingual",
  "version": "1.0.0",
  "description": "Free, open-source bilingual reader for X/Twitter, Reddit and YouTube — line-by-line original + translation, plus on-demand whole-page translation.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "zip": "node scripts/zip.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.43",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}